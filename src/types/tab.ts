export interface tabStudent {
    tabActive: string,
    setTabActive : (tab: string) => void
}
export interface tabTeacher {
    tabActive: string,
    setTabActive : (tab: string) => void
}
export interface tabAdmin {
    tabActive: string,
    setTabActive : (tab: string) => void
}